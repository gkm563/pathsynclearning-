import type { ReactNode } from "react";

export const dynamic = "force-dynamic";

/** Authenticated student product (onboarding + portal). */
export default function AppLayout({ children }: { children: ReactNode }) {
  return children;
}
