/**
 * @deprecated Import `PortalShell` only from the portal Next.js layout.
 * This alias exists so accidental legacy imports fail the ESLint restricted-import rule
 * the same way as PortalShell.
 */
export { default } from "./PortalShell";
